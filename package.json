{
  "name": "@codeation/acc",
  "private": true,
  "scripts": {
    "start": "lerna run start --stream",
    "bootstrap": "lerna run bootstrap --stream",
    "build": "lerna run build --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "lerna": "^3.20.2"
  }
}
